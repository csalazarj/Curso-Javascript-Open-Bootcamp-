const nombre = "Cristhian"
const apellido = "Salazar"

const estudiante = `${nombre} ${apellido}`

const estudianteMayus = estudiante.toUpperCase()

const estudianteMinus = estudiante.toLowerCase()

const len = estudiante.length

const primera = nombre[0]

const ultima = apellido[apellido.length - 1]

const sin_espacios = estudiante.trim()

const boo = estudiante.includes(nombre)
