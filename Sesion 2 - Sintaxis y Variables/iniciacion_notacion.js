// Notación

// ; --> Delimitar el final de una linea
const b = 2;
b + 4;

// . --> Se utliza en los objetos apra acceder a los atributos
o = {
  vel: 0,
};

console.log("o.vel = ", o.vel);

// [] --> listas, arreglos o arrays
const ar = [1, 1, 3, 5];
console.log("ar[2] = ", ar[2]);

// () --> Funciones
function suma(a, b) {
  // función
}

// {} --> Llaves para objetos, funciones y estructuras de control
const movil = {
  largo: 5,
  alto: 6,
};

if (true) {
  // algo
}
